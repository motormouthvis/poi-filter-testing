# Copy to .env and fill in. .env is gitignored — never commit real credentials.
# Base site URL (no trailing slash), e.g. https://app.dreamneighborhood.com
POI_ADMIN_BASE_URL=
POI_ADMIN_USERNAME=
POI_ADMIN_PASSWORD=
